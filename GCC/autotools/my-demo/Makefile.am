

ACLOCAL_AMFLAGS = 
bin_PROGRAMS = run
run_SOURCES = app/main.c src/foo.c include/foo.h

run_CPPFLAGS = -g -O0 -Werror -Wall

clean-local:
	rm aclocal.m4
	rm -rf autom4te.cache
	rm compile
	rm configure
	rm depcomp
	rm install-sh
	rm Makefile.in
	rm missing
	
